# 运行时数据库（SQLite，含 WAL 文件，含真实兑换码数据，绝不入库）
data/

# 依赖（本项目零外部依赖，保留以防后续扩展）
node_modules/

# 日志
*.log

# 环境变量（含 ADMIN_TOKEN / DEMO_CODE 等敏感配置，绝不入库）
.env
.env.local
.env.*.local

# 系统文件
.DS_Store
Thumbs.db
